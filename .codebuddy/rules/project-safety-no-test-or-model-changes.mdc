---
description: 
alwaysApply: true
enabled: true
updatedAt: 2026-02-28T00:54:41.242Z
provider: 
---

### 项目规则（始终生效）

1. **不修改测试代码逻辑**：不得更改任何用于评测/测试的脚本、断言、期望输出、测试用例逻辑；若需要适配，只能修改被测实现或提示词内容。
2. **不修改调用的模型和参数**：不得更改任何脚本中 `chat(...)`/模型调用处的 `model` 字段与推理参数（例如 `options`、`temperature` 等）。
3. **不修改代码中已定义的函数**：不得更改代码中已存在的函数定义（包括函数签名与函数体逻辑）。
4. **System prompt 禁止硬编码实际结果**：不得在 `system prompt` 中硬编码 `actual result`/期望输出/最终答案等会导致作弊的内容。
